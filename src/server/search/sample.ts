/**
 * NODE FACE of the space sampler. The module moved whole to
 * src/store/search/sample.ts in Phase 5 of the browser port — it was always
 * environment-neutral (mulberry32 over the axis product); only its address
 * changed. Every WHY lives on the module itself.
 */
export * from '../../store/search/sample';
